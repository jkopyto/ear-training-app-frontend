import { defineMessages } from 'react-intl'

const messages = defineMessages({
    nextExcersise: {
        id: "next-excersise",
        defaultMessage: "Next Excersise"
    },
    backToMain: {
        id: "back-to-main",
        defaultMessage: "Back to dashboard"
    }
})

export default messages